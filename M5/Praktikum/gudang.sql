create or replace function insertBrgFDS
--nmbrg in barang.nama%type, satuan in barang.satuan%type, 
--hrg in barang.hargabeli%type, kodegol in barang.kodegol%type)

return VARCHAR2 is
nid varchar2(10);

cursor cl is 

begin
--case 
--    when barang.kodebrg like 'FDS%' then 
--    insert into barang values ();
end;